__pycache__/
*.pyc
.venv/

# Heavy / generated training artifacts — keep folder structure, not contents
data/raw/*
!data/raw/PUT_IMAGES_HERE.txt
data/labels/
data/pose/
data/preview/
data/needle/
data/arm-pose-converted/
data/phlebotomy-src/
data/downloads/
markers/
*.onnx

# Trained checkpoints + run metadata (args.yaml, results.csv, curves, weights)
# are tracked so the fine-tuned model ships with the repo.
!runs/**/*.pt
